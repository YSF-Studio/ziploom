{
  "name": "ziploom",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "tauri": "tauri",
    "tauri:dev": "node scripts/ensure-dist.mjs && tauri dev",
    "tauri:build": "node scripts/ensure-dist.mjs && tauri build && node scripts/package-releases.mjs",
    "package:releases": "node scripts/package-releases.mjs",
    "icons": "tauri icon src-tauri/icons/logo.svg -o src-tauri/icons",
    "test:gui": "npm run build && node tests/gui-smoke.mjs",
    "test:ipc": "node tests/ipc-coverage.mjs",
    "test:e2e": "cargo test --manifest-path src-tauri/Cargo.toml --tests --locked",
    "test:forensic": "cargo test --manifest-path src-tauri/Cargo.toml --test forensic_exam_test -- --nocapture",
    "test:all": "npm run test:ipc && npm run test:e2e && npm run test:gui",
    "screenshots": "npm run build && node tests/capture-screenshots.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-fs": "^2"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5",
    "@tauri-apps/cli": "^2",
    "playwright": "^1.60.0",
    "svelte": "^5",
    "vite": "^6"
  }
}
